Installation
============

``Le AB`` is intended to work with Python 3.6 or above.

Installation can be done by using ``pip`` :

.. code-block:: bash

    pip install leab

There are `wheels available <https://pypi.org/project/leab/#files>`_ for Linux, MacOS, and Windows.